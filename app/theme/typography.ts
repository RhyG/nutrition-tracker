const weights = {
  light: 100,
  semiLight: 300,
  medium: 500,
  semiBold: 700,
  bold: 800,
} as const;

export const typography = {
  /**
   * The fonts are available to use, but prefer using the semantic name.
   */
  weights,
};
